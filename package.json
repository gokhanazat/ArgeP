{
  "name": "argep",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "export JAVA_HOME=$JAVA_HOME_17_X64 && export PATH=$JAVA_HOME/bin:$PATH && chmod +x gradlew && ./gradlew :webApp:wasmJsBrowserDistribution --no-daemon",
    "build:win": "cmd /c gradlew :webApp:wasmJsBrowserDistribution --no-daemon && if not exist public mkdir public && xcopy /E /I /Y webApp\\build\\dist\\wasmJs\\productionExecutable\\* public\\"
  }
}
